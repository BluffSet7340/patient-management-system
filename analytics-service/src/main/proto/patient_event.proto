syntax='proto3';

package patient.events; // all generated code goes into this package or folder
option java_multiple_files = true; 

// this is what gets consumed by the kafka consumer
message PatientEvent { // data sent dependent on business logic
    string patientId=1;
    string name=2;
    string email=3;
    string event_type=4;
}